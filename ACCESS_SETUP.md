# 앱별 이름과 접속 비밀번호

Vercel 프로젝트의 Settings → Environment Variables에서 설정한 뒤 다시 배포한다.

| 환경변수 | 용도 | 미설정 시 |
| --- | --- | --- |
| `APP_DISPLAY_NAME` | 메인·로그인 화면의 `HELLO 이름` | `DAEHO` |
| `APP_PASSWORD` | 앱 접속 비밀번호. 20자 이상의 무작위 비밀번호 권장 | 비밀번호 없이 기존처럼 접속 |

두 값 모두 서버 환경변수다. `NEXT_PUBLIC_` 접두사는 붙이지 않는다.
공유용 프로젝트에는 반드시 `APP_PASSWORD`를 설정한다. 실제 비밀번호를 저장소에 커밋하지 않는다.

- 비밀번호 자체는 브라우저에 저장하지 않는다. 서명된 로그인 쿠키를 최대 365일 유지한다.
- 쿠키는 HttpOnly, SameSite=Lax, 운영 환경에서 Secure를 사용하며 해당 호스트에만 적용된다.
- 같은 기기라도 브라우저가 다르거나 쿠키 삭제, 시크릿 모드, 만료 시에는 다시 입력한다.
- `APP_PASSWORD` 변경 후 재배포하면 기존 쿠키는 다음 서버 요청부터 무효화된다. 이미 열린 화면의 표시 내용을 원격으로 지우지는 않는다.
- 페이지, DB 조회, 모든 매매 Server Action, AI API에 접근 검사를 적용한다.
- 자동 시세 수집은 기존 `CRON_SECRET` 인증을 유지한다. 로그인 쿠키로는 cron 인증을 대신할 수 없다.
- 로그인 실패는 서버 인스턴스·IP별 10분간 10회로 제한한다. 서버리스 전체에 걸친 강제 제한이 아니므로, 운영에서 더 강한 방어가 필요하면 Vercel Firewall의 로그인 경로 제한을 별도로 설정한다.
- 기존 페이지 재방문 캐시 정책은 유지한다. 로그인 성공 시 전체 페이지 이동으로 로그인 전 캐시와 분리한다.
- 이름과 비밀번호 변경에 DB 스키마 수정이나 데이터 이전은 필요 없다.

검증: `node --import tsx --test src/lib/access/*.test.ts`, `npm run build`, `npm run typecheck`.
